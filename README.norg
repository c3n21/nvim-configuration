* Issues
** Astro
   - [ ] `before_init` doesn't get merged when specified in `lsp/astro.lua`
   - [ ] To avoid `Buffer  file://<file>  newer than edits.` I need to re-enter the buffer. Maybe I should check VSCode extension code.
   -- To reproduce it:
   ~~~ create a new Astro component
   ~~~  use it inside another component 
   ~~~ try to use the code action to import it
   ~~~ error
** Dotnet
   Needs a nix shell nixpkgs#<framework version> to have it to work properly, otherwise EasyDotnet doesn't work

* TODO
- [ ] Check if the issues are caused by nixCats.
